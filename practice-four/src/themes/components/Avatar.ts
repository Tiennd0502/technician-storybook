import { defineStyleConfig } from '@chakra-ui/react';

export const Avatar = defineStyleConfig({
  sizes: {
    md: {
      container: {
        width: 'md',
        height: 'md',
      },
    },
  },
});
